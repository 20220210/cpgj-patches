# Resin Dual-P2C R6 server download package

Download this branch as ZIP, extract it, then run:

```bash
bash install.sh
```

`install.sh` reconstructs the verified `resin-dual-p2c-r6.tar.gz`, checks SHA-256, extracts the deployment package, and executes `deploy.sh`.
